# HARNESS_CHANGELOG.md — folksound-atlas

原則: **エージェントがミスをするたびに、そのミスが二度と起きないようハーネスを改良する。**
起票条件: 同一失敗コード累計 2 回(LL-10)、または severity S1(LL-12)。

フリート正本は `../harness-kit/HARNESS_CHANGELOG.md`。本ファイルは本プロジェクト発の起票を先に置き、
承認後に正本へ還流する。採番は追記の直前に正本の最大値を確認する。

## 採番の衝突と、ずらした記録(2026-09-08)

起票時(2026-09-07)に正本を見た時点の最大は **HC-203** だったので HC-204〜207 を採った。
ところが本プロジェクトの作業中に**別のセッションが並行して起票**しており、
作業を終えて見直した時点で正本は **HC-225** まで伸びていて、HC-204〜207 は
別プロジェクト(tarp-shelter-lab ほか)の記録に埋まっていた。
そこで本ファイルの採番を次の空き番へずらした:

| 起票時 | 現在 | 内容 |
|---|---|---|
| HC-204 | **HC-226** | 外部データ源は SPEC に書く前に 1 件取って測る / 帰属を返す API は別経路で検算する |
| HC-205 | **HC-227** | 許可リストは実データ全域に当てて「残りかす」を目で見る / 対照は経路まで固定する |
| HC-206 | **HC-228** | 図は「在るか」でなく層ごとに数える / 座標系を変えたら読む側を数え直す |
| HC-207 | **HC-229** | 手元は通るがデプロイ先だけ失敗するときは「何が届いたか」を疑う |

**`logs/loops/*.jsonl` の `harness_ref` は古い番号のまま残してある。**
追記専用のログを事後に書き換えないためで(LL-00a)、
記録した時点ではその番号が正しかった。上の表で読み替えること。

なお **採番の衝突自体がフリートの再発事象**である(go-parallel-web-crawler でも同日に 2 度衝突した)。
番号は「起票の直前に見た最大値 + 1」でしかなく、**並行作業がある限り一意性は保証されない。**
還流のときに正本側で採り直す前提で扱うのが安全である。

---

## HC-226

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-09-07 |
| トリガー | `DATA-SRC` 累計 2 回目・3 回目(LL-10、folksound-atlas loop_000)。**同じループで、外部データ源が三つの別々のやり方で期待を裏切った。** (a) 仕様書が最優先と名指しした Library of Congress は Cloudflare の対話型チャレンジ配下にあり、`robots.txt` すら HTML を返し、JSON API は 403。(b) 代替に選んだ Wikimedia Commons では、95 カテゴリへ `deepcat:` 検索を連続発行して HTTP 429。(c) **その `deepcat:` が、誤りを黙って返していた** —— `deepcat:"Folk music of Ireland" filemime:audio` は `totalhits=112` を返すが、上位の実体はスウェーデンの SMV 音源で、当該ファイルのカテゴリを直接引くと**アイルランド系カテゴリに一切属していない**。警告もエラーも無い |
| 追加診断 (c) | (c) は (a)(b) と質が違い、**いちばん危ない**。取得は成功し、HTTP 200 が返り、件数は 112 という**もっともらしい数**で、警告フィールドも空である。**「取れたか」を測るだけの検査は、これを全部通す。**しかも国別件数表という成果物は、間違っていても形は正しく見える —— アイルランド 112、スウェーデン 430 と並んだ表を見て、中身が全部スウェーデンだと疑う理由はどこにも無い。気づけたのは、**件数ではなく実体(ファイル名)を目で見た**からで、しかも「アイルランドを引いたのにスウェーデン語の地名が出た」という**言語の食い違い**という偶然に助けられている。日本とインドを引いていたら気づかなかった可能性が高い。**教訓は「件数オラクルは、件数を数えた対象が何であるかを検算しない」**である。地理・分類・所属のような**帰属**を外部の便利な演算子から取るときは、その演算子の答えを**別経路の権威**(ここでは `list=categorymembers` による明示的な所属)で少なくとも数件突き合わせ、一致しなければ演算子ごと捨てる |
| 診断 | (a)(b) は表面上べつの失敗だが、根は一つで、**「そのエンドポイントが自分にとってどう振る舞うかを、計画の前に一度も測っていない」**ことである。(a) は仕様書に書いてあるという理由だけで主データ源として計画に載せた。実際には**この機からは一件も取れない**。(b) は「検索 API」という一般名から通常の検索と同じコストを勝手に仮定した。`deepcat:` は**サーバ側でカテゴリ木を展開する**ので、見た目は同じ `list=search` でも桁違いに重い。**どちらも、着手前に 1 件だけ叩いてみれば分かった。**とくに (a) が高くつく型で、SPEC・ETL・画面までを書いてから気づくと、書いた分がまるごと無駄になる。フリートでは jomon-lens が同じ型を踏んでおり(仕様書の主データ源が robots.txt で Disallow)、**「仕様書に書かれた外部データ源は、書かれているというだけでは使えるかどうか何も言っていない」**が繰り返し確認されている |
| 改訂 | agents_core の「外部データの採録(HC-012)」へ次を追加することを提案する: **「外部データ源は、SPEC に書く前に *その源から実際に 1 件取得して* 可否を測る。測る対象は (1) 取得できるか(認証・チャレンジ・robots.txt・403/429)、(2) 1 件あたりの所要と、こちらが出す予定の総リクエスト数で律速しないか、(3) 必要な項目(本件では権利表示と地理帰属)が**その 1 件に実在するか**、の三つである。仕様書・先行例・一般名からの類推で代用してはならない —— **同じ名前の API でも演算子ひとつでコストが変わる**。測った結果は SPEC に実測日つきで書き、取得不能だった源は『採らない』と理由つきで残す(消すと、後から同じ源が再提案される)」**。さらに (c) を受けて: **「外部 API の便利な演算子・集約機能から *帰属*(所属・分類・地理・親子関係)を取るときは、その答えを別経路の権威と数件突き合わせてから採用する。件数が返ったこと・エラーが無かったことを根拠にしてはならない —— **帰属を誤る API は、正しい形の、もっともらしい数を返す**。突き合わせは実装前に手で行い、一致した事実と実測日を SPEC に残す」** |
| 種別 | agents_md |
| SCAFFOLD_VERSION | (承認後に採番) |
| 効果検証 | 以後 10 ループで「SPEC に書いた外部データ源が実は取得不能・律速で使えない」型の失敗 0 件なら Closed |
| propagation | 未実施(folksound-atlas SPEC §2.1 / §5 が先行事例) |
| 状態 | Proposed |
| 備考 | 失敗レコードは `logs/loops/loop_000.jsonl`。以後の `DATA-SRC` 記録には `harness_ref=HC-226` を付ける |

## HC-227

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-09-07 |
| トリガー | `VERIF-GAP` 累計 2 回目(LL-10、folksound-atlas loop_000)。(a) ライセンス判定器の**陽性対照が結論だけを見ていて経路を見ていなかった** —— 拒否リストを空にする変異体を当てたところ、NC/ND 系は「許可リストに無い」で `unknown` となり、`allowed` のみを見る 10 件が全部素通りした(落ちたのは `family` を見る 1 件だけ)。(b) その許可リストの正規表現が**法域移植版(ported)を落としていた** —— `CC BY 3.0 us` / `CC BY-SA 2.0 de` / `CC BY-SA 3.0 cl` が `unknown` になっていた |
| 診断 | 二つとも根は**「検査に使った例を自分で書いた」**ことにある(HC-068 の系)。(a) では、自分で並べた禁止例が「不許可になる」ことだけを確かめ、**なぜ不許可になったか**を見なかった。許可リスト方式では、禁止リストが死んでも許可リストが拾って不許可になるので、`allowed` だけを見る対照は**二重の防御に守られて発火しない**。(b) では、自分の知っている表記だけを例に並べたので、`CC BY 3.0 us` のような**実データにしか出てこない形**が例に入らなかった。どちらも「合成した例で緑」を「検査した」と読み替えている。**決定的だったのは、実データ 5,492 件を全部判定器に通し、`unknown` の中身を目で見たこと。** 許可リスト方式は、判定できないものを `unknown` に貯める —— **この残りかすが所見そのもの**であり、`unknown` を黙って通す実装にしていたら、この 5 件は「許可」として静かに混ざり、誰も気づかなかった |
| 改訂 | agents_core の品質ゲートへ次を追加することを提案する: **「許可リスト・分類器・正規化表を書いたら、*実データの全域*に当てて『どれにも当たらなかったもの』を必ず目で見る。その残りかす(`unknown` / `other` / 未分類)は捨て場ではなく所見であり、件数だけでなく中身を列挙してから、足すか・落とすかを決める。**そして残りかすを黙って通す道を作らない** —— 通す実装では、この検査自体が成立しない」。あわせて陽性対照について: **「対照は結論(合否)ではなく *どの経路で* そうなったかまで固定する。多段の防御があるとき、結論だけを見る対照は、段が一つ死んでも緑のままになる」**(HC-065 の、多段防御への適用) |
| 種別 | agents_md |
| SCAFFOLD_VERSION | (承認後に採番) |
| 効果検証 | 以後 10 ループで「合成例では緑だが実データの形で落ちる分類器・正規化表」の失敗 0 件なら Closed |
| propagation | 未実施(folksound-atlas `ml/src/folksound/rights.py` と `ml/tests/test_rights.py` が先行事例) |
| 状態 | Proposed |
| 備考 | 失敗レコードは `logs/loops/loop_000.jsonl`。採番は追記の直前に正本の最大値(HC-203)と本ファイル(HC-226)を確認した |

## HC-228

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-09-08 |
| トリガー | `GEN-LAYOUT` 累計 2 回目(LL-10、folksound-atlas loop_001 / loop_002)。同じ一枚の図で、**別々の原因で二度**壊れた。(a) 下地の GeoJSON を `./data/world.geojson` と相対で引いており、`trailingSlash: true` の `/map/` 配下では `/map/data/...` を叩いて 404。**陸地が一枚も描かれないのに、型検査もビルドも単体テストも全部緑**だった。(b) その後、緯度を `[-58,84]` に切って `viewBox` を `1000×420` に縮めたが、**描画側を切らなかった**ため南極のポリゴンが `y=428` から高さ 87 ぶん `viewBox` の外へ出ていた |
| 診断 | 二つとも「**図が出ていること**」と「**図が正しいこと**」を取り違えたことから来ている。(a) は在存の問題だが、`<svg>` 自体は在り、丸(国の点)も出ていたので、**頁としては完成して見えた**。欠けていたのは `<path>` だけで、地図を知らない検査からは「ちゃんと描かれている」と区別が付かない。(b) はもっと質が悪く、**自分で座標系を変えたのに、その座標系を使う側を数え直さなかった**。`viewBox` は図の約束であって、描画データはその約束を知らない —— 約束を縮めたら、はみ出す者が出るのは当然なのに、変更したのは投影式だけだった(HC-070「アルゴリズムを差し替えたら既存の不変量の根拠を再導出する」の、図への適用)。**どちらも頁の横溢れ検査では捕まらない。**横溢れは図の外側しか見ないからである。実際に効いたのは (1) 描画要素を**種類ごとに数える**検査(`path` が 0 面なら落とす)と (2) `getBBox()` と `viewBox` を比べる幾何検査の二つだけだった |
| 改訂 | agents_core の品質ゲート(HC-159 の項)へ次を追加することを提案する: **「図には『在るか』ではなく『どの種類の要素が何個あるか』を検査する。背景・軸・データ点は在るのに、肝心の層(地図の陸地・折れ線・棒)だけが 0 個という壊れ方をするので、層ごとに下限を置く」**。あわせて: **「図の座標系(投影・スケール・domain・`viewBox`)を変えたら、その座標系を読む側を全部数え直す。変えた側だけを直すと、はみ出し・切れが静かに残る。座標系を変えたループの完了条件に、`getBBox()` と `viewBox` の突き合わせを必ず入れる」** |
| 種別 | agents_md |
| SCAFFOLD_VERSION | (承認後に採番) |
| 効果検証 | 以後 10 ループで「図の層が丸ごと欠ける / 座標系変更で図がはみ出す」型の失敗 0 件なら Closed |
| propagation | 未実施(folksound-atlas `scripts/verify-browser.mjs` の `checkSvgGeometry` / 層ごとの計数が先行事例) |
| 状態 | Proposed |
| 備考 | 失敗レコードは `logs/loops/loop_001.jsonl` と `loop_002.jsonl` |

## HC-229

> **⚠ 2026-09-09 訂正: 下の「診断」は誤りだった。** 角括弧ディレクトリの取りこぼしではなく、
> **`.vercelignore` の `data/` が無印パターンで `public/data/` まで除外していた**のが真因である。
> `.gitignore` と同じ意味でパターンはどの階層にも当たる。`public/data/songs.json` が消えると
> `generateStaticParams()` が読むものを失い、Next は
> 「関数が無い」と報告する。先頭 `/` で固定したら**リモートビルドのデプロイが通った**(実測)。
> **教訓そのものは残すが、原因の当て方を間違えた記録として残す** —— 下の「訂正から得た教訓」を参照。

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-09-08(訂正 2026-09-09) |
| トリガー | `TOOL-ENV` 累計 2 回目(LL-10、folksound-atlas loop_003)。(a) `vercel deploy` が `Upload aborted` で落ちた —— `.vercelignore` を置いておらず、音源 881MB と `.venv` を丸ごと送ろうとしていた。(b) `.vercelignore` を置いたら今度は**リモートのビルドだけ**が `Page "/song/[id]" is missing "generateStaticParams()"` で失敗した。**手元の `npm run build` は 314 頁を生成でき、当該ファイルは `git ls-files` にも出る。**角括弧を含むディレクトリ(`src/app/song/[id]/`)が CLI の転送側で取りこぼされたと判断し、prebuilt デプロイ(`vercel build --prod --yes` → `vercel deploy --prebuilt --prod`)へ切り替えて成功した |
| 診断 | (b) が本題である。**「手元でビルドが通る」と「配られる木でビルドが通る」は別**(HC-062)だが、この件はさらに一段深く、**「配られる木」そのものが手元と違っていた**。転送の除外規則はグロブで書かれるため、`[id]` のような**動的ルートの角括弧が文字クラスとして解釈されうる**。しかも失敗の出方が紛らわしい —— Next は「ファイルが無い」ではなく「`generateStaticParams()` が無い」と言うので、**コードの誤りに見える。**実際こちらは一瞬、関数の書き方を疑った。`git ls-files` と手元ビルドの二つを突き合わせて初めて「送られていない」に行き着いた。動的ルートは現代のフレームワークではありふれているので、この罠は誰でも踏む |
| 改訂 | agents_core の「この環境で書くとき」へ次を追加することを提案する: **「デプロイ先のビルドだけが失敗し、手元のビルドが通るときは、コードを疑う前に *その木に何が届いたか* を疑う。とくに角括弧・空白・非 ASCII を含むパス(動的ルート `[id]`、`[...slug]` など)は、転送の除外規則がグロブとして解釈して落とすことがある。確かめ方は (1) `git ls-files` に在るか (2) 手元の同じコマンドで通るか の二点を突き合わせること。届いていないと分かったら、**手元で作った成果物をそのまま配る**(Vercel なら `vercel build` → `vercel deploy --prebuilt`)に切り替えるのが速い —— これは検品した木をそのまま配ることにもなり、二重に得である」**。あわせて: **「大きな中間生成物(音源・データセット・仮想環境)を持つプロジェクトでは、最初のデプロイの前に除外設定を書く。書かないと転送が中断し、原因が容量だと分かるまで時間を取られる」** |
| 種別 | agents_md |
| SCAFFOLD_VERSION | (承認後に採番) |
| 効果検証 | 以後 10 ループで「手元は通るがデプロイ先だけ失敗する」型の失敗 0 件なら Closed |
| propagation | 未実施(folksound-atlas `.vercelignore` と prebuilt 運用が先行事例) |
| 状態 | Proposed |
| 備考 | 失敗レコードは `logs/loops/loop_003.jsonl`。訂正の失敗レコードは `loop_008.jsonl` |

### HC-229 の訂正から得た教訓(2026-09-09)★

**回避策が効いてしまうと、誤った診断が検証されないまま残る。**

角括弧説を立てたあと prebuilt デプロイに切り替えたら通ったので、
「やはり転送の問題だった」と読んでしまった。だが prebuilt が通るのは
**ローカルでビルド済みの成果物を送るから**であって、
`.vercelignore` の効き方とは関係がない。つまりあの成功は仮説を支持していない。

しかも**否定する証拠はその場にあった**。GitHub 連携のビルドでも同じ失敗が出ており、
そちらは Vercel がリポジトリを clone するので CLI の転送は一切介在しない。
角括弧説はその時点で否定されていたのに、
「prebuilt で回避できた」ことに満足して確かめに行かなかった。

したがって agents_core へ次を提案する:
**「回避策で先へ進めたときは、診断が確かめられたわけではない。
回避策の成功は、たいてい仮説と無関係な理由で起きる。
原因を HC やドキュメントに書き残すなら、*回避策ではなく原因のほうを直して直ることを一度見る*。
それができないなら『未特定』と書く —— 誤った原因を書くほうが、
空欄より後の判断を狂わせる」**。あわせて:
**「無印の ignore パターンはどの階層にも当たる(`.gitignore` / `.vercelignore` / `.npmignore` 共通)。
`data/` は `public/data/` にも当たる。除外設定は先頭 `/` で固定して書く」**

## HC-230

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-09-08 |
| トリガー | `SPEC-DRIFT` 累計 2 回目(LL-10、folksound-atlas loop_002 / loop_005)。(a) SPEC §2.4 に「投稿者 63 人」と書いたが実際の出力は 122 人で、**画面を目視して初めて**気づいた。(b) `schemas/*.schema.json` を L0 で書いたあと exporter にフィールドを足し(`songs.audio_url` / `models.generated_at` / `features.excerpt_policy`)、`additionalProperties: false` で 3 ファイルとも不適合になっていた。**どちらも作業中ずっと気づかず、テストは全部緑だった** |
| 診断 | 二つは「文書の数」と「文書の形」という違いはあるが、根は同じで、**宣言したものを誰も当てていなかった**ことである。テストは実装を守るが、**SPEC に書いた数も、schemas に書いた形も、それ自体は何も守っていない**。しかも両方とも「書いた時点では正しかった」ため、書き手には正しさの記憶が残る —— これが厄介で、後から読み返しても疑う理由が湧かない。(b) がとくに示唆的で、**スキーマは書いただけだと「守られている」という錯覚を生む**。`additionalProperties: false` という厳しい宣言を置いてあったのに、当てていなかったので一度も発火しなかった。SPEC のゲート表が実装でも検査でもないのと同じ構造である(HC-157) |
| 改訂 | agents_core の品質ゲートへ次を追加することを提案する: **「宣言した契約(JSON Schema・型定義・ゲート表・文書中の数値)は、*出荷物に実際に当てる検査*を同じループで用意する。当てていない契約は、守られているかどうかについて何も言っていない —— しかも書き手には『書いた』記憶が残るので、後から疑われにくい。とくに (1) スキーマは実データに当てて初めて検査になり、壊した文書を落とす陽性対照を対で置く (2) 文書に数値を書いたら、その数を生んだ出力から機械で読み直して突き合わせる検査を置く。突き合わせられない書き方(散文に埋め込む等)しかできないなら、それは守れない数だということでもある」** |
| 種別 | agents_md |
| SCAFFOLD_VERSION | (承認後に採番) |
| 効果検証 | 以後 10 ループで「宣言した契約と出荷物の食い違いを、目視でしか見つけられない」型の失敗 0 件なら Closed |
| propagation | 未実施(folksound-atlas `ml/tests/test_schema.py` と `ml/tests/test_docs_match_data.py` が先行事例) |
| 状態 | Proposed |
| 備考 | 失敗レコードは `logs/loops/loop_002.jsonl` と `loop_005.jsonl`。採番は追記の直前に正本の最大値(HC-225)と本ファイル(HC-229)を確認した |
