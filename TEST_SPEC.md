# TEST_SPEC.md — folksound-atlas

<!-- scaffold template v1.30.0 から展開(2026-09-07) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- **仮想環境は本プロジェクト専用の `.venv` を使う。** この機はシェルの `VIRTUAL_ENV` が
  別プロジェクト(juchu-desk)を指したままになることがある(HC-171)。
  依存を入れる前・テストを走らせる前に `sys.prefix` を読んで確かめる(T-000)
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威 | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | 実測日と実測値をコメントに残す。母集団も併記する(HC-152) |

**件数・行数は定数で書かない。** 集合の一致・取りこぼしの不在という不変量で書く。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| Commons `extmetadata` のライセンス文字列 | 外部権威(API 応答・取得日を記録) | 非循環。権利判定の唯一の根拠 |
| Commons カテゴリ `Folk music of <国>` | 外部権威(人手の帰属) | 非循環。地理の唯一の根拠。**推定で補完しない** |
| 合成正弦波・白色雑音 | 解析解 | 音響特徴量の閉形式検算(rms / zcr / centroid) |
| 同一音声の二経路(Python / TS) | 二実装照合 | G-07。**経路(中間量)まで比べる**(HC-065) |
| 置換検定の帰無分布 | 自前生成(seed 固定) | H-01 の対照。観測が帰無から外れて初めて主張できる |

## ケース一覧

**「対応要求」には SPEC の ID を必ず書く**(HC-157)。

「状態」欄は **今そのケースが実在するか**を書く。予定を「済」と書かない。

| ID | 対応要求 | ケース | 期待 | 状態 |
|---|---|---|---|---|
| T-000 | 実行規約 | `sys.prefix` が本プロジェクトの `.venv` 配下である | 一致 | ✅ `test_env.py` |
| T-001 | F-01 / **G-01** | 許可すべきライセンス表記が通る(誤検出 0) | 違反 0 件 | ✅ `test_rights.py` |
| T-002 | **G-01** | 陽性対照: 禁止・未知の表記を**期待した経路で**落とす | **必ず落ちる** | ✅ `test_rights.py` |
| T-003 | F-02 / **G-02** | 出荷する全件が `source_url` / `retrieval_date` / `sha256` を持つ | 違反 0 件 | ✅ `test_dataset.py`(出荷前は skip) |
| T-004 | **G-02** | 陽性対照: `source_url` を欠いた行を混ぜる | **必ず落ちる** | ✅ `test_dataset.py` |
| T-005 | F-03 | mono 化が平均であり、出力が float32 である | 一致 | ✅ `test_preprocess.py` |
| T-006 | F-03 | セグメント長・ホップが SPEC §7 の値と一致する | 一致 | ✅ `test_preprocess.py` |
| T-007 | F-04 | 合成正弦波の rms / zcr / centroid が閉形式と一致する | 閉形式一致 | ✅ `test_features.py` |
| T-008 | F-04 | 陰性対照: 無音・定数信号で非有限を出さない | 例外なし | ✅ `test_features.py` |
| T-009 | §9 / **G-03** | 同一録音のセグメントが二つの split に現れない | 違反 0 件 | ✅ `test_split.py` |
| T-010 | **G-03** | 陽性対照: セグメント単位のランダム分割を検査器に通す | **必ず落ちる** | ✅ `test_split.py` |
| T-011 | §2.2 / **G-04** | オートエンコーダの `forward`/`encode` が**署名として**ラベルを取れない | 違反 0 件 | ✅ `test_models.py` |
| T-012 | **G-04** | 対照: 分類器の側が `n_classes` を必要とし、AE とは別物である | 成立 | ✅ `test_models.py` |
| T-012b | **G-04** | `models.json` の `saw_country_labels=true` が地理解析から除外されている | 違反 0 件 | ✅ `test_dataset.py` |
| T-025 | §5.4 | 国名の正規化(`Italia`→`Italy`、冠詞落とし)と、触らない例の陰性対照 | 一致 | ✅ `test_countries.py` |
| T-026 | F-12 | 地理距離が閉形式(子午線 1/4・対蹠点・緯度 1 度)と一致する | 相対誤差 < 1e-9 | ✅ `test_geo.py` |
| T-013 | F-06 / **G-06** | UMAP の乱数種とパラメータが出力に書き出されている | 存在 | ✅ `test_dataset.py`(二度実行の一致は ⬜) |
| T-014 | **G-05** | 配布 JSON の id 集合が songs.json と整合し、非有限が無い | 違反 0 件 | ✅ `test_dataset.py` |
| T-015 | F-07 | Embedding が L2 正規化 / Top-N が降順で自己を含まない | 違反 0 件 | ✅ `test_dataset.py` |
| T-027 | **G-08 / G-09** | Mantel と偏 Mantel が、仕込んだ相関を検出し、無関係では有意にならない | 陽性・陰性とも成立 | ✅ `test_mantel.py` |
| T-016 | F-04 / **G-07** | Python と TS の特徴量が一致する。**中間量まで比べる** | 閾値は実測後 | ⬜ L3 |
| T-017 | **G-07** | 陽性対照: 経路だけずらした TS 実装で照合が落ちる | **必ず落ちる** | ⬜ L3 |
| T-018 | F-12 / **G-08** | H-01 の置換検定が走り、p 値と観測統計量が出る | **成否は問わない**。出力の存在と再現性 | ⬜ L4 |
| T-019 | F-15 / **G-08** | 画面の H-01 判定文が検定結果 JSON から導出されている | 手書き文字列が無い | ⬜ L4 |
| T-020 | **G-09** | H-02 の対照(録音出自の説明力)が算出され出力される | 出力の存在 | ⬜ L4 |
| T-021 | N-05 / **G-10** | 日本語本文に別字種・制御文字が無い | text_hygiene 合格 | ✅ `harness/text_hygiene.py` |
| T-022 | **G-11** | 図の要素の境界矩形が `viewBox` に収まる | はみ出し 0 件 | ⬜ L5 |
| T-023 | **G-12** | 実ブラウザ検品(複数幅)で横溢れ無し・図が描画される | 検品器が緑 | ⬜ L5 |
| T-024 | **G-12** | 検品器の陽性対照: 壊した画面を検品器に通す | **必ず落ちる** | ⬜ L5 |

## 未実装のゲート(SPEC §10 と一致させること)

未実装は **G-07(Python と TS の二実装照合)のみ**。
本アプリは特徴量を TS 側で再計算せず Python の出した JSON を描くだけなので、
照合する相手がまだ存在しない(将来ブラウザ内で計算するなら必要になる)。

G-01 / G-02 / G-03 / G-04 / G-05 / G-06 / G-08 / G-09 / G-10 / G-11 / G-12 は実装済み。
この一覧と SPEC §10 は、どちらかを直したらもう一方も直す。

## 追加したケース(L2 以降)

| ID | 対応要求 | ケース | 状態 |
|---|---|---|---|
| T-028 | **G-05** | 配布 JSON を `schemas/*.schema.json` に当てる | ✅ `test_schema.py` |
| T-029 | **G-05** | 陽性対照: 必須欠落・型違い・const 違反の 3 通りで落ちる | ✅ `test_schema.py` |
| T-030 | **G-06** | UMAP が同じ種で完全一致し、違う種では別配置になる | ✅ `test_projection.py` |
| T-031 | F-04 | 既知テンポのクリック列でテンポが値として当たる | ✅ `test_features.py` |
| T-032 | F-04 | 陽性対照: 別テンポで別の値が出る(定数を返していない) | ✅ `test_features.py` |
| T-033 | 文書 | SPEC の件数・置換設定・相関表が出力と一致する | ✅ `test_docs_match_data.py` |
