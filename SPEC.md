# SPEC.md — folksound-atlas(FolkSound Atlas)

<!-- scaffold template v1.30.0 から展開(2026-09-07)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

世界の民謡・フォークソングのうち、**権利状態を一件ずつ確かめられたもの**だけを集め、
波形・スペクトログラム・従来型音響特徴量・深層学習 Embedding を通して
「音の構造」から地理を眺めるデジタル・アトラスを作る。

中心にある問いは一つである。

> **地理的に近い音楽は、音響的にも近いのか。**

本プロジェクトはこの問いに「はい」と答えることを目的にしない。
**測って、答えが何であれそれを画面に出す**ことを目的とする。

---

## 2. この SPEC が V1.1 仕様書から変更した点(実測にもとづく)

仕様書(FolkSound Atlas V1.1 完全実装仕様書)を出発点とするが、
L0 の実測で前提が崩れた箇所を以下のとおり改める。**変更理由は実測である。**

### 2.1 主データ源の変更(仕様書 §33 → 本 SPEC §5)

仕様書 §33 は Library of Congress(Citizen DJ / National Jukebox)を最優先データ源とする。
**この経路は本機から自動取得できない(実測 2026-09-07)。**

- `https://www.loc.gov/robots.txt` — Cloudflare の対話型チャレンジ HTML が返る
- `https://www.loc.gov/collections/national-jukebox/?fo=json` — **HTTP 403**

したがって LoC は**採らない**。代替は §5 に定める。

### 2.2 学習タスクの変更(仕様書 §47 → 本 SPEC §8)★最重要

仕様書 §47 は自作 CNN の初期実装を **「Classification(地域・カテゴリ分類)+ Embedding 抽出」**
とする。いっぽう仕様書 §92 は研究的問いとして
**Q1「地理的に近い民謡は音響的にも似ているか」**、
**Q5「音響空間のクラスタは文化的・地理的境界と一致するか」** を掲げる。

**この二つを同時に採ると循環する。** 国ラベルで学習した Embedding が国ごとに固まるのは
**構成上の必然**であって、観測ではない。循環したオラクルは緑のまま何も検査しない(HC-045)。

よって本 SPEC は Embedding を**ラベルを見たかどうか**で二分し、
**地理の主張に使ってよいのはラベル非依存の系だけ**とする(§8 / G-04)。

| 系統 | 国ラベルを見たか | 地理の主張に使ってよいか |
|---|---|---|
| 従来型音響特徴量(librosa) | 見ていない | ✓ |
| 自作 CNN(自己教師あり・再構成) | 見ていない | ✓ |
| 事前学習済みモデル(外部・本データのラベル未使用) | 見ていない | ✓ |
| 自作 CNN(国分類器) | **見た** | **✗(対照としてのみ表示)** |

国分類器はアプリから消さない。**「答えを教わった側」として並べて見せる**ことが、
循環とは何かを読者に示す最も分かりやすい方法だからである。画面上でそう明示する(F-14)。

### 2.3 目玉の事前登録と、外し方の宣言

仕様書は Q1 を「探索可能にする」とだけ書く。本 SPEC は**結論の方向を先に登録する**。

- **H-01(目玉)**: 地理距離と音響距離のあいだに正の相関がある(Mantel 置換検定・両側 p<0.05)
- **H-02(対照)**: H-01 が成り立つとき、それは**録音の出自**(収録年代・帯域・アップロード元・
  デジタル化バッチ)で説明されない

**H-01 が落ちたら落ちたと画面に書く。** H-02 が落ちた場合、H-01 は
「地理を測った」ではなく「録音条件を測った」を意味するので、**目玉は成立しない**ものとして扱う。
歴史的録音の寄せ集めでは、CNN が真っ先に拾うのは楽曲構造ではなく**録音機材と帯域**である
——これは想定される最も有力な対抗仮説であり、対照を置かずに H-01 を主張してはならない。

---

## 3. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 権利状態を一件ずつ検証した音源のみからデータセットを構築する | must |
| F-02 | 各録音に出自(source / source_url / license / license_url / 取得日 / sha256)を保持する | must |
| F-03 | 音声を単一の内部標準(WAV / mono / 22050Hz / float32)へ正規化する | must |
| F-04 | 従来型音響特徴量(rms, zcr, spectral_*, mfcc, chroma, tempo)を算出・保存する | must |
| F-05 | 自作 CNN(自己教師あり)を学習し 128 次元 Embedding を出す | must |
| F-06 | Embedding を UMAP / PCA で 2 次元へ射影する | must |
| F-07 | コサイン類似度による音響的近傍(Top-N)を出す | must |
| F-08 | 世界地図(MapLibre GL JS)に録音地点を出す | must |
| F-09 | Sound Space(2 次元散布図)を出し、地図と相互に連動させる | must |
| F-10 | 曲詳細で 波形 / スペクトログラム / 特徴量 / 近傍 を出す | must |
| F-11 | 音声を再生できる(HTML5 Audio) | must |
| F-12 | 地理距離 vs 音響距離の散布図と相関を出す | must |
| F-13 | 複数モデルの Embedding 空間を切り替えて比較できる | should |
| F-14 | 各モデルが**国ラベルを見たか**を画面に明示する | must |
| F-15 | H-01 / H-02 の検定結果を、成否にかかわらず画面に出す | must |
| F-16 | 静的サイトとして Vercel に配信する(サーバー側計算なし) | must |

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 課金経路ゼロ(GitHub / Vercel Hobby の無料枠のみ) | デプロイ構成の目視 + 関数・cron 数 0 |
| N-02 | 学習・特徴量抽出は手元で行い、Vercel では一切計算しない | `output: "export"` の静的書き出し |
| N-03 | 配布 JSON の総量を抑える(Embedding の全次元を素で配らない) | ビルド後の実測(実測後に置き直す) |
| N-04 | 再現可能(dataset / model / feature 版と乱数種を manifest に記録) | manifest の存在と再実行の一致 |
| N-05 | 日本語本文に別字種・制御文字が混入しない | `python harness/text_hygiene.py` |

## 5. データ源(仕様書 §33 の差し替え)

### 5.1 採用する源

| 源 | 役割 | 地理の出所 | 権利の出所 |
|---|---|---|---|
| Wikimedia Commons | 主 | カテゴリ `Folk music of <国>` 等(**人が付けた**) | `extmetadata` の LicenseShortName / UsageTerms |
| Internet Archive(78rpm・年 ≤ 1925) | 補 | 未定(採用可否は L1 で判断) | 米国録音の公有化年(§5.3) |

**地理を題名・演奏者名から推定しない**(HC-012)。国は
**Commons のカテゴリという人手の帰属**からのみ取る。カテゴリが無いものは採らない。

### 5.2 権利ゲート(仕様書 §35–§38 を踏襲・強化)

次のいずれかに当たる録音は**公開データセットから除外**する。

- `rights_verified = false`
- ライセンスが `unknown` / 判定不能
- ライセンス文字列が許可リストに無い

許可するのは **Public Domain 系 / CC0 / CC BY / CC BY-SA** のみ。
NC(非営利限定)・ND(改変禁止)は、特徴量抽出という改変を伴うため**採らない**。

**録音(Sound Recording)と原曲(Underlying Work)は別に確かめる**(仕様書 §37)。
民謡は原曲が伝承(著作者不明・古典)であることが多いが、**編曲・採譜・訳詞に権利が残りうる**。
両方が確認できたものだけ `rights_verified = true` とする。

### 5.3 米国録音の公有化(補データ源を採る場合のみ)

Music Modernization Act(2018)Title II により、米国で発行された録音は
1923 年以前が 2022-01-01 に公有化し、以後は発行から 100 年で順次公有化する。
**2026-09-07 時点では 1925 年以前発行の米国録音が公有**である。
この規則を使う場合、判定は発行年のみに依らせず、§5.2 の二重確認を併せて行う。

## 5.4 走査の実測(2026-09-07)

`ml/scripts/01_discover.py` を深さ 2 で走らせた結果(API 2,478 回):

| 測ったもの | 値 |
|---|---|
| 候補となった音源(重複除去後) | **5,492 件** |
| 1 件以上あった国 | **38 国** |
| 走査した国カテゴリ | 94 |

国別の上位と、偏りの実態:

| 国 | 件数 | 全体比 |
|---|---:|---:|
| スウェーデン | 3,660 | 66.6% |
| フィンランド | 1,170 | 21.3% |
| アメリカ合衆国 | 161 | 2.9% |
| イギリス | 112 | 2.0% |
| ウクライナ | 63 | 1.1% |
| エストニア | 48 | 0.9% |
| ドイツ | 46 | 0.8% |
| (以下 31 国) | 232 | 4.2% |

**上位 2 国で 87.9% を占める。** これは「その国に民謡が多い」ことではなく、
**その国の公的アーカイブが Commons へ大量に投稿した**ことを意味する。
そのまま使うと H-01 は「北欧の二つのアーカイブが同じ機材で録ったか否か」を測ってしまうので、
**国ごとに上限を設けて均す**(§6)。均したのであって代表にしたのではない。

さらに実測で分かったこと:

- **カテゴリ名は揺れる。** `Italy`(14 件)と `Italia`(12 件)が別の国名として立つ。
  突き合わせの前に正規化が要る(§5.5)
- **1 件しか無い国が 8 国**ある。分類の学習には使えないが、地図と地理解析には載せる

### 権利の実測(2026-09-07)

候補 5,492 件を判定した結果:

| 判定 | 件数 |
|---|---:|
| Public Domain 系 | 5,103 |
| CC BY-SA | 361 |
| CC BY | 25 |
| **許可 合計** | **5,489** |
| 拒否(`Copyrighted free use` / `GFDL`) | 2 |
| 未知(`EEF OAL-1`) | 1 |

落ちた 3 件は `data/raw/rights_summary.json` に名指しで残す。
**未知は「たぶん大丈夫」で通さない。**

### 投稿者の偏り —— H-02 が要る理由(実測 2026-09-07)★

許可された 5,489 件の投稿者は 205 人。ところがその内訳は:

| 投稿者 | 件数 | 全体比 |
|---|---:|---:|
| `AliciaFagervingWMSE-bot` | 3,713 | 67.6% |
| `Alicia Fagerving (WMSE)` | 1,118 | 20.4% |
| (以下 203 人) | 658 | 12.0% |

**上位 2 アカウントで 88.0% を占め、どちらも Wikimedia Sverige(WMSE)の一括投稿である。**
そしてこの 4,831 件は、国別の上位 2 国(スウェーデン 3,660 + フィンランド 1,170 = 4,830)と
ほぼ一致する。つまり本コーパスでは

> **「国」と「どのアーカイブがデジタル化したか」がほとんど同じものを指している。**

この状態で H-01(地理距離 vs 音響距離)に相関が出ても、それは
**「同じアーカイブが同じ機材で録ったか否か」**を測った可能性が高い。
したがって:

1. **国ごとに上限を設けて均す**(§6)。これは交絡を消しはしないが、薄くする
2. **H-02 の対照を必ず併走させる**(G-09)。投稿者を説明変数にしたときの説明力を測り、
   国より投稿者のほうがよく説明するなら、**H-01 は成立しないものとして扱う**

## 5.5 座標の出所

国の座標は **Wikidata の P625(coordinate location)** から取る(`04_geocode.py`)。
自分で緯度経度を書かない。

**この座標は国の代表点であって、録音された場所ではない。**
`coordinate_precision` は `country_centroid` とし、地理距離もこの粒度を超えない。
名前が当たらなかった国は `unmatched` として残し、**推測で埋めない**(仕様書 §59)。

## 6. データセット規模

| 段階 | 目標 |
|---|---|
| MVP | 100–500 録音(仕様書 §39) |
| 拡張 | 1,000+ |

**選抜規則**: 国ごとに上限 20 件(乱数種 42 で無作為)。
上限は偏りを均すためであり、超えた国から何件落としたかは manifest に残す。
到達した件数は L1 の実測で確定する(**実測後に置き直す**)。

**件数はテストに定数で書かない**(HC-016)。集合の一致・取りこぼしの不在で書く。

## 7. 音声の内部標準(仕様書 §28–§31 を踏襲)

```
Format: WAV / Channels: Mono / Sample Rate: 22050 Hz / dtype: float32
Segment: 5.0 sec / Hop: 2.5 sec
Mel: n_fft=2048, hop_length=512, n_mels=128, fmin=20, fmax=8000
MFCC: n_mfcc=40
```

原音は変更せず保存する。短い録音には適応的処理を行う(規則は L1 で定め、ここへ戻す)。

## 8. モデル構成(仕様書 §13–§19 の差し替え)

| model_id | 種別 | 国ラベル | 地理の主張 |
|---|---|---|---|
| `feat-baseline-v1` | 従来型特徴量の標準化ベクトル | 見ていない | 可 |
| `self-ae-v1` | 自作 CNN オートエンコーダ(再構成のみ) | 見ていない | 可 |
| `self-clf-v1` | 自作 CNN 国分類器 | **見た** | **不可(対照)** |
| `pretrained-*` | 外部事前学習モデル | 見ていない | 可(採否は L6 で判断) |

`models.json` に `saw_country_labels: true|false` を必ず持たせ、
画面はこの値から表示を出す(F-14)。**画面の断り書きを手書きしない。**

## 9. データリーク対策(仕様書 §27)

同一録音から作った複数セグメントを train/val/test へランダム分割してはならない。
**録音単位で分割する**(70 / 15 / 15)。

さらに本 SPEC は次を足す:
**同一アップロード元・同一デジタル化バッチが分割をまたぐ場合、それは漏れではないが交絡である。**
H-02 の対照はこの交絡を測るためにある。

## 10. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | 公開データセットの全件が `rights_verified = true` かつ許可ライセンス | 違反 0 件 |
| G-02 | 公開データセットの全件が `source_url` と取得日を持つ | 違反 0 件 |
| G-03 | セグメント分割が録音単位である(同一録音が二つの split に現れない) | 違反 0 件 |
| G-04 | **地理の主張に使う Embedding が国ラベルを見ていない**(循環の禁止) | 違反 0 件 |
| G-05 | 配布 JSON が schema に適合する | 違反 0 件 |
| G-06 | UMAP が乱数種込みで再現する | 二度実行して一致 |
| G-07 | 二実装照合: Python の特徴量計算と TS 側の再実装が一致する | 閾値は L 実測後に定める |
| G-08 | **H-01(目玉)**: 地理距離と音響距離の相関が置換検定で有意 | p<0.05(**落ちてもよい。落ちたら画面に書く**) |
| G-09 | **H-02(対照)**: H-01 が録音の出自で説明されない | 判定規則は L5 で定める |
| G-10 | 日本語本文に別字種・制御文字が無い | text_hygiene 合格 |
| G-11 | 図の要素が `viewBox` に収まる(HC-159) | はみ出し 0 件 |
| G-12 | 実ブラウザ検品(複数画面幅)を通る | 検品手順で確認 |

**ゲートは宣言であって実装ではない**(HC-157)。各 `G-xx` は TEST_SPEC のケースから
ID で参照されるか、ここに「未実装」と明記されるかのいずれかでなければならない。

現時点で**実装済み**のゲート: G-01(`test_rights.py`)/ G-03(`test_split.py`)/
G-04(`test_models.py` が署名を検査)/ G-10(`harness/text_hygiene.py`)。

現時点で**未実装**のゲート: **G-02 / G-05 / G-06 / G-07 / G-08 / G-09 / G-11 / G-12**。
この一覧と TEST_SPEC の「未実装のゲート」は、どちらかを直したらもう一方も直す。

## 11. 画面(仕様書 §103 を踏襲)

```
/            Home
/map         World Map
/space       Sound Space
/song/[id]   Song Detail
/models      Models(ラベルを見たかを含む比較表)
/about       About(出自・権利・限界)
```

## 12. スコープ外

- **音源の大量配信**。Vercel には小容量のデモ音源とメタデータ・特徴量・Embedding のみを置く(仕様書 §71)
- **Vercel 上での学習・推論**(仕様書 §74)
- CLAP による自然言語検索(仕様書 §128 の V1.2 候補)
- 3D Sound Space / 多言語 UI / 旋律・リズムの記号的解析(仕様書 §129 の V2.0 候補)
- **音楽的な優劣・真正性の判定**。本アプリは音響的な近さを測るだけであり、
  ある録音がその文化を「代表する」とは主張しない
